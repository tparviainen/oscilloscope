using System;

namespace Scope
{
    class Program
    {
        static void Main(string[] args)
        {
            /*
             * scope -p <plugins> -i <interface ip:p> -c command [arguments] -c ...
             * scope idn
             * scope display_data <color> <invert> <format>
             */

            Console.WriteLine("Scope!");
        }
    }
}
